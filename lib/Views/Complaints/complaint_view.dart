import 'package:flutter/material.dart';

class ComplainceView extends StatelessWidget {
  const ComplainceView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Complaints"));
  }
}
