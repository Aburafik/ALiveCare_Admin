import 'package:flutter/material.dart';

class PharmacyStoreView extends StatelessWidget {
  const PharmacyStoreView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Pharmacy Store"),
    );
  }
}